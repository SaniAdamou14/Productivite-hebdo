# Productivite-hebdo

Weekly-goals variant of the [Productivite](https://github.com/SaniAdamou14/Productivite) Node.js/Express app - same authentication and JSON-file-backed API, adapted for weekly goal tracking.

## Setup

```bash
npm install
npm start   # reads config from .env (PORT, USERS_FILE, GOALS_FILE)
```
